/** Golden-hour helpers — port from README `tick` / sun-derived utilities. */
export function getGoldenHourCountdownLikeLegacy(_now: Date): unknown {
  void _now;
  throw new Error("getGoldenHourCountdownLikeLegacy not ported — see README.md tick()");
}

export interface PanZoomConfig {
  padL: number;
  padR: number;
}

/**
 * Canvas pan/zoom coordinator — port logic from `attachPanZoom` in README.
 */
export function attachPanZoomLikeLegacy(
  _canvas: HTMLCanvasElement,
  _cfg: PanZoomConfig
): void {
  void _canvas;
  void _cfg;
}
